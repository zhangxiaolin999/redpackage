server:
  port: 8081
  servlet:
    context-path: /open/api

limit:
    minMoney: 0.01
    maxMoney: 9999,
    description: 最少要发${limit.minMoney}, 最多不能超过${limit.maxMoney}
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://0.0.0.0:3306/redpack
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true